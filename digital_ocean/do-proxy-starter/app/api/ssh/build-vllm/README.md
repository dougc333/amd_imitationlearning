this takes too long

is it easier to scp install.sh then poll to see if vllm directory is created and an import works?

install_nodialog.sh works when copy paste code into terminal logged into instance. Verifies no dialog. 


Keep install_nodialog.sh for reference. 


